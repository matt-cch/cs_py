# ========================================
# 策略：默认排除所有，渐进式放行
# ========================================

# === 1. 排除所有根级子目录（白名单模式） ===
/*/

# === 2. 渐进式放行：只保留 references/tasks/deploy-git-isolated/ ===
!/references/
/references/*
!/references/tasks/
/references/tasks/*
!/references/tasks/deploy-git-isolated/
!/references/env-migrations/

# ========================================
# 全局安全屏蔽（始终生效，不受白名单影响）
# ========================================

# 环境变量与密钥（最高优先级）
.env
.env.*
*.key
*.pem
*-key.txt
*-key.pem
*secret*
*token*
*password*
credentials*

# 隔离工具链（不应上传）
venv/
venv/tmp/
venv/data-*/
venv/cache-*/
venv/state-*/

# 编辑器本地配置（含绝对路径）
.vscode/settings.json
.cursor/settings.json
.idea/

# 调试与临时产物
debug/
out/
logs/
*.log

# 构建输出与依赖锁定（按需开放）
__pycache__/
*.pyc
node_modules/

# 真源检测报告（可能含本地路径）
references/runtime/verify-runtime-report-*.json

# 技能锁定文件（运行时生成）
skills-lock.json

# 其他
*.zip
*.7z
*.tar.gz
